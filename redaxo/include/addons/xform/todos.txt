Todos:

Manager
- popup relations, javascript em files etc.. 
- Feldtypen für Datenbank definierbar machen

config

	function rex_editme_assets($params){
		$params['subject'] .= "\n  ".'<script src="../files/addons/editme/em.js" type="text/javascript"></script>';
		return $params['subject'];
	}
	rex_register_extension('PAGE_HEADER', 'rex_editme_assets');

be_medialist etc. noch einsetzen...
ist in _save

Überall bei den Values in das <p> id="'.$this->getHTMLId().'" einsetzen

- gruppe - be_relation.. hidden, fields fest eintragen ... 
	- popup mit nur einem eintrag benutzen
	- popup mit mehrfachauswahl aber ohne zielfelder
	- popup mit mehrfachdaten in der zieltabelle mit eigenem zielfeld.

xform, community addon
	- feste felder einführen für login/email/etc.., die nicht verändert werden können
	- datenbanktyp sollte nachtraeglich geaendert werden bei generate.. wenn schon vorhanden, passiert das nicht

- xform
	- layout xform
	-- einbauen ###label### ###field###
	- select_sql select klasse aufnehmen
	-- noch als multiple erweitern

